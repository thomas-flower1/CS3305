using UnityEngine;

public class Objectives : MonoBehaviour
{


    // Update is called once per frame
    void Update()
    {
        
    }

    void read_all()
    {
        // function that reads all the files from the textfiles folder - displays them all to the screen
        
    }
}
